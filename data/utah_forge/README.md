Place Utah FORGE laboratory shear experiment files here.

Expected files:
- `0_README.txt`
- one or more `*_datatable.mat` files
- optional roughness `.cag` files

The Utah FORGE pipeline expects the README plus the MATLAB datatable files so it can infer time, stress, displacement, and velocity columns.

